Restaurant Management System
A comprehensive web application built with Django to streamline restaurant operations, including menu management and customer interaction.

🚀 Key Features
Menu Management: Allows administrators to add, update, and categorize food items dynamically.

Order Processing: A system for managing incoming orders and tracking their status.

Responsive UI: Built using HTML, CSS, JavaScript, and Bootstrap for a seamless experience on both mobile and desktop.

Database Integration: Utilizes Django's ORM for efficient data handling and storage.

🛠️ Tech Stack
Backend: Python, Django

Frontend: HTML5, CSS3, JavaScript, Bootstrap

Database: SQLite (default Django DB)

📦 Installation and Setup
To run this project locally, follow these steps:

Clone the repository:

Bash
git clone https://github.com/Angelvij/Restaurant_management.git
Navigate to the project directory:

Bash
cd Restaurant_management
Install dependencies:

Bash
pip install -r requirements.txt
Apply migrations:

Bash
python manage.py migrate
Run the server:

Bash
python manage.py runserver
